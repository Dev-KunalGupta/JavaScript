let value = Math.floor(Math.random() * 10)+1
console.log(value)

console.log(typeof Date)       // Date is a constructor function in JavaScript.

let date = new Date()          // new Date() creates an instance of the Date object.
console.log(typeof(date));     // object

// In JavaScript:
// Constructors (like Date, Array, Object) → "function"
// Instances (like new Date(), [], {}) → "object"

const str = 'kunal'
console.log(str.length);